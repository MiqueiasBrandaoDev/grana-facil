# Configurações para Deploy Real
# Copie este arquivo para .env e configure os valores

# URL do webhook de deploy na VPS
VITE_DEPLOY_WEBHOOK_URL=http://deploy.granaboard.com.br/deploy

# Chave secreta para autenticação do deploy (deve ser a mesma da VPS)
VITE_DEPLOY_SECRET=sua-chave-secreta-super-segura-aqui

# Exemplo de configuração para produção com SSL:
# VITE_DEPLOY_WEBHOOK_URL=https://deploy.granaboard.com.br/deploy